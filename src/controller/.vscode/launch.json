{
    "configurations": [
        {
            "name": "controller-launch",
            "type": "node-terminal",
            "request": "launch",
            "preLaunchTask": "build-controller",
            "command": "cd ${workspaceFolder}/../.. && source install/setup.bash && ros2 run controller controller",
        }
    ]
}